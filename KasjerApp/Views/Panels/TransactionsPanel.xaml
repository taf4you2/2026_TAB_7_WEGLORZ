<UserControl x:Class="KasjerApp.Views.Panels.TransactionsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Historia transakcji"
                   FontSize="22" FontWeight="Bold" Foreground="#1565C0" Margin="0,0,0,16"/>

        <!-- Filtry -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/></Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Data:" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                <DatePicker x:Name="DateFilter" Height="36" Width="160" FontSize="13"/>
                <Button Content="Szukaj" Click="SearchBtn_Click"
                        Background="#1565C0" Foreground="White" BorderThickness="0"
                        Padding="14,0" Margin="10,0,0,0" Height="36" Cursor="Hand"/>
                <Button Content="Wyczyść filtr" Click="ClearBtn_Click"
                        Background="#78909C" Foreground="White" BorderThickness="0"
                        Padding="14,0" Margin="8,0,0,0" Height="36" Cursor="Hand"/>
                <TextBlock x:Name="MsgText" VerticalAlignment="Center"
                           Margin="16,0,0,0" FontSize="12" Foreground="#C62828"
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8">
            <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/></Border.Effect>
            <DataGrid x:Name="TransGrid"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      RowBackground="White" AlternatingRowBackground="#F9FAFB"
                      BorderThickness="0" CanUserResizeRows="False"
                      HeadersVisibility="Column" ColumnHeaderHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"           Binding="{Binding Id}"                                  Width="60"/>
                    <DataGridTextColumn Header="Typ operacji" Binding="{Binding OperationType}"                        Width="140"/>
                    <DataGridTextColumn Header="Taryfa"       Binding="{Binding Tariff}"                              Width="160"/>
                    <DataGridTextColumn Header="Kwota (PLN)"  Binding="{Binding Amount, StringFormat=N2}"             Width="110"/>
                    <DataGridTextColumn Header="Data"         Binding="{Binding Date, StringFormat=dd.MM.yyyy HH:mm}" Width="140"/>
                    <DataGridTextColumn Header="Kasjer"       Binding="{Binding CashierLogin}"                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
